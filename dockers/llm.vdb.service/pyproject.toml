[project]
name = "createvectordb"  # Required
version = "1.0.0"  # Required
description = "Create FAISS vectordb, serialize and upload to S3"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = ["common", "config", "createvectordb", "createvectordb_jira_csv_local"]